services:
  neo4j:
    image: neo4j:5.26.29-community
    environment:
      NEO4J_AUTH: ${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:?Set NEO4J_PASSWORD}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:?Set NEO4J_PASSWORD}
    ports:
      - "127.0.0.1:7474:7474"
      - "127.0.0.1:7687:7687"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "cypher-shell -u neo4j -p \"$${NEO4J_PASSWORD}\" 'RETURN 1' >/dev/null || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
    volumes:
      - neo4j-data:/data
    restart: unless-stopped

volumes:
  neo4j-data: